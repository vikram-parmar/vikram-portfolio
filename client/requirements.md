## Packages
framer-motion | Page transitions and scroll-triggered animations
react-hook-form | Form state management
@hookform/resolvers | Zod validation resolver for forms
lucide-react | Premium scalable icons
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts

## Notes
Requires a tailwind configuration that supports dark mode class or simply dark colors by default. We'll enforce a strictly dark theme via CSS variables.
Fonts used: 'Syne' for headings and 'Plus Jakarta Sans' for body text.
